package com.infosupport.labs.ch1

// - create a class Article, with id and description, and an optional price (default 0.00)
// - create a setOf a few articles
// - print the set of articles: are the articles shown in a readable format? why?
// - show that duplicates are not allowed in the set (add two "same" articles: do they both appear in the set?)
// - show the cheapest article
// - show the ids of the articles with a given description, or "not found" otherwise
